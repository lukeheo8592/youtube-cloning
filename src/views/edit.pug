extends base.pug

block content
    h4 Change Title of vidoe
    form(method="POST")
        input(name="title", placeholder="videoTitle",value=video.title, required)
        input(value="Save",type="submit") 